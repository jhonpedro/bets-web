import styled from 'styled-components'

export const AccountContainer = styled.div`
  margin-top: 4rem;
  color: var(--color-dark);

  strong {
    font-size: 3rem;
    display: block;
  }
  span {
    font-size: 1.5rem;
  }
`
